import ContactClient from "./_client";

export const metadata = {
  title: "Contact",
  description:
    "Contact the Helios Pro team — sales, support, partnerships and press. Email, phone, live chat, or send us a message.",
};

export default function ContactPage() {
  return <ContactClient />;
}
