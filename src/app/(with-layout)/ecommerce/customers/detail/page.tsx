import ClientPage from "./_client";

export const metadata = {
  title: "Customer Detail",
  description: "Helios Pro ecommerce — customer profile, lifetime value, order history and notes.",
};

export default function Page() {
  return <ClientPage />;
}
