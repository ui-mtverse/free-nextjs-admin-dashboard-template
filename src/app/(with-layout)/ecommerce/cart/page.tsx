import CartClient from "./_client";

export const metadata = {
  title: "Cart",
  description: "Helios Pro ecommerce — review your cart, apply promo codes and proceed to checkout.",
};

export default function CartPage() {
  return <CartClient />;
}
