import AdvancedInputsClient from "./_client";

export const metadata = {
  title: "Advanced Inputs",
  description:
    "Helios Pro — input groups, prefix/suffix, phone + currency masks, tags input, OTP input, password strength meter and an autocomplete combobox.",
};

export default function AdvancedInputsPage() {
  return <AdvancedInputsClient />;
}
