import SwitchesClient from "./_client";

export const metadata = {
  title: "Switches",
  description:
    "Helios Pro — basic, labelled, sized, coloured, icon and grouped-panel switch controls.",
};

export default function SwitchesPage() {
  return <SwitchesClient />;
}
