import SelectsClient from "./_client";

export const metadata = {
  title: "Selects",
  description:
    "Helios Pro — single, multi, grouped, searchable and avatar variants for select controls.",
};

export default function SelectsPage() {
  return <SelectsClient />;
}
