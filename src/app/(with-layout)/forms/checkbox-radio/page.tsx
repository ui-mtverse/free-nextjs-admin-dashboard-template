import CheckboxRadioClient from "./_client";

export const metadata = {
  title: "Checkbox & Radio",
  description:
    "Helios Pro — basic, grouped, inline, cards-as-radio and toggle-style checkbox/radio controls.",
};

export default function CheckboxRadioPage() {
  return <CheckboxRadioClient />;
}
