import DatePickerClient from "./_client";

export const metadata = {
  title: "Date Picker",
  description:
    "Helios Pro — single date, range, time, inline calendar and quick presets using flatpickr.",
};

export default function DatePickerPage() {
  return <DatePickerClient />;
}
