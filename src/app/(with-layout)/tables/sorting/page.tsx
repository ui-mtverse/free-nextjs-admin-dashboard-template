import ClientPage from "./_client";

export const metadata = {
  title: "Sortable Table | Tables | Helios Pro",
  description: "DataTable with multiple sortable columns including text, number, currency, date, and progress values.",
};

export default function Page() {
  return <ClientPage />;
}
