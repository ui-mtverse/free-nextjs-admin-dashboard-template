import ClientPage from "./_client";

export const metadata = {
  title: "Sticky Header & Column | Tables | Helios Pro",
  description: "DataTable with a sticky header that stays in view while scrolling vertically, plus a sticky first column.",
};

export default function Page() {
  return <ClientPage />;
}
