import PaginationClient from "./_client";

export const metadata = {
  title: "Pagination",
  description:
    "Helios Pro — pagination component showcase: basic, with edges, with jump, sizes and simple variants.",
};

export default function PaginationPage() {
  return <PaginationClient />;
}
