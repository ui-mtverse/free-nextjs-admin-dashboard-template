import ProgressClient from "./_client";

export const metadata = {
  title: "Progress",
  description:
    "Helios Pro — progress component showcase: linear, circular, multi-segment and stepped progress indicators.",
};

export default function ProgressPage() {
  return <ProgressClient />;
}
