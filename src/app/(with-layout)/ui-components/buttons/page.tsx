import ButtonsClient from "./_client";

export const metadata = {
  title: "Buttons",
  description:
    "Helios Pro — button component showcase: all variants, sizes, icons, loading, disabled, groups and split buttons.",
};

export default function ButtonsPage() {
  return <ButtonsClient />;
}
