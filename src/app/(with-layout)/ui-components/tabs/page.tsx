import TabsClient from "./_client";

export const metadata = {
  title: "Tabs",
  description:
    "Helios Pro — tabs component showcase: underline, pills and boxed variants with icons, badges and vertical orientation.",
};

export default function TabsPage() {
  return <TabsClient />;
}
