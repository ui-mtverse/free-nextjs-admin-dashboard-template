import AccordionsClient from "./_client";

export const metadata = {
  title: "Accordions",
  description:
    "Helios Pro — accordion component showcase: basic, with descriptions, multiple open, with icons, flush style and custom chevron.",
};

export default function AccordionsPage() {
  return <AccordionsClient />;
}
