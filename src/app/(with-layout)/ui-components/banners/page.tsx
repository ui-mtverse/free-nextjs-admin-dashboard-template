import BannersClient from "./_client";

export const metadata = {
  title: "Banners",
  description:
    "Helios Pro — banner component showcase: info, success, warning, danger, primary banners with actions, dismissible, sticky-top and inline variants.",
};

export default function BannersPage() {
  return <BannersClient />;
}
