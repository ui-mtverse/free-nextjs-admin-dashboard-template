import ModalsClient from "./_client";

export const metadata = {
  title: "Modals",
  description:
    "Helios Pro — modal component showcase: basic, header/footer, four sizes, confirmation, form and danger modals.",
};

export default function ModalsPage() {
  return <ModalsClient />;
}
