import AlertsClient from "./_client";

export const metadata = {
  title: "Alerts",
  description:
    "Helios Pro — alert component showcase: info, success, warning, danger, primary alerts with icons, actions and dismissible states.",
};

export default function AlertsPage() {
  return <AlertsClient />;
}
