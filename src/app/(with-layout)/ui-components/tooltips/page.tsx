import TooltipsClient from "./_client";

export const metadata = {
  title: "Tooltips",
  description:
    "Helios Pro — tooltip component showcase: top, bottom, left, right, rich HTML content and a delayed variant.",
};

export default function TooltipsPage() {
  return <TooltipsClient />;
}
