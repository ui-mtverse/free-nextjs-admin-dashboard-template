import DropdownsClient from "./_client";

export const metadata = {
  title: "Dropdowns",
  description:
    "Helios Pro — dropdown menus built on the Popover primitive: with icons, dividers, headers, multi-level, user menu and notifications.",
};

export default function DropdownsPage() {
  return <DropdownsClient />;
}
