import PopoversClient from "./_client";

export const metadata = {
  title: "Popovers",
  description:
    "Helios Pro — popover component showcase: basic, with header, with actions, aligned (start/center/end) and menu-style popovers.",
};

export default function PopoversPage() {
  return <PopoversClient />;
}
