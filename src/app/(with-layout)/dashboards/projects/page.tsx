import ClientPage from "./_client";

export const metadata = {
  title: 'Projects Dashboard',
  description: 'Helios Pro projects dashboard — kanban, burndown, team load and activity.',
};

export default function Page() {
  return <ClientPage />;
}
