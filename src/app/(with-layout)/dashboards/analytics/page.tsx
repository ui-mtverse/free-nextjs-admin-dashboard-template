import ClientPage from "./_client";

export const metadata = {
  title: 'Analytics Dashboard',
  description: 'Helios Pro analytics dashboard — sessions, engagement, conversion and traffic sources.',
};

export default function Page() {
  return <ClientPage />;
}
