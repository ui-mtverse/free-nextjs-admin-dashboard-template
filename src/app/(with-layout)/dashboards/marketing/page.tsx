import ClientPage from "./_client";

export const metadata = {
  title: 'Marketing Dashboard',
  description: 'Helios Pro marketing dashboard — campaigns, channels, funnel and spend.',
};

export default function Page() {
  return <ClientPage />;
}
