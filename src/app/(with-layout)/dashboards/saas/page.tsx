import ClientPage from "./_client";

export const metadata = {
  title: 'SaaS Dashboard',
  description: 'Helios Pro SaaS dashboard — MRR, churn, trials and accounts.',
};

export default function Page() {
  return <ClientPage />;
}
