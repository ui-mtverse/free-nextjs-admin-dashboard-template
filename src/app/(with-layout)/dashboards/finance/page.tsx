import ClientPage from "./_client";

export const metadata = {
  title: 'Finance Dashboard',
  description: 'Helios Pro finance dashboard — revenue, expenses, OHLC and transactions.',
};

export default function Page() {
  return <ClientPage />;
}
