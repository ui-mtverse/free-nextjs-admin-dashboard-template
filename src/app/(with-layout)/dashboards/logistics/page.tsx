import ClientPage from "./_client";

export const metadata = {
  title: 'Logistics Dashboard',
  description: 'Helios Pro logistics dashboard — shipments, on-time, carriers and dispatches.',
};

export default function Page() {
  return <ClientPage />;
}
