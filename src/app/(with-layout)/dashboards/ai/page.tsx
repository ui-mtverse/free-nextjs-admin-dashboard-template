import ClientPage from "./_client";

export const metadata = {
  title: 'AI Dashboard',
  description: 'Helios Pro AI dashboard — model usage, tokens, latency and inference.',
};

export default function Page() {
  return <ClientPage />;
}
