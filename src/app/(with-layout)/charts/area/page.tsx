import ClientPage from "./_client";

export const metadata = {
  title: 'Area Charts',
  description: 'Helios Pro area chart variants — basic, gradient, stacked, spline, range.',
};

export default function Page() {
  return <ClientPage />;
}
