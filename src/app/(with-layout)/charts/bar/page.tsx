import ClientPage from "./_client";

export const metadata = {
  title: 'Bar Charts',
  description: 'Helios Pro bar chart variants — vertical, horizontal, stacked, grouped, 100% stacked.',
};

export default function Page() {
  return <ClientPage />;
}
