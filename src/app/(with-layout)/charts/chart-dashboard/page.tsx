import ClientPage from "./_client";

export const metadata = {
  title: 'Chart Dashboard',
  description: 'Helios Pro chart dashboard — multiple chart types in one page.',
};

export default function Page() {
  return <ClientPage />;
}
