import ClientPage from "./_client";

export const metadata = {
  title: 'KPI Cards',
  description: 'Helios Pro KPI card variants — stat, sparkline, ring, delta, mini bar.',
};

export default function Page() {
  return <ClientPage />;
}
