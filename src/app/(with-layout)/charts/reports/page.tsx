import ReportsClient from "./_client";

export const metadata = {
  title: "Reports",
  description:
    "Helios Pro reports library — browse, view and export scheduled reports with charts and tables.",
};

export default function ReportsPage() {
  return <ReportsClient />;
}
