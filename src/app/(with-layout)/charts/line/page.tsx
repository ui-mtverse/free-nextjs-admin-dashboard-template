import ClientPage from "./_client";

export const metadata = {
  title: 'Line Charts',
  description: 'Helios Pro line chart variants — basic, multi-series, smoothed, dashed, markers, annotations.',
};

export default function Page() {
  return <ClientPage />;
}
